Pet compare needs to be able to compare abilities of the same tier
would team (and compare in general, like in pet picker) compare be more effective if it automatically compared the like abilities between the teams, if any (including diff tier) instead of just seemingly the first ability
Team summary card of team compare, on the left side the team score and team name are misaligned (they are currently aligned to centre instead of left)
'All Strategies' selection dropdown box need to be reworked to reflect the new compare logic better (display the actual groups)
pet picker compare needs values trunicated like expected/trigger and value/trigger
team badges need to be looked at /revamped to be more visually appealling and representing the game truly (correct colors, proper ability badge (like pet optimiser has) with custom text/values to as the badges)
in pet teams, shift doesnt work as a modifier key for the pet team keybind?
in the feeding tab, when pet feed buttons are detached, they must be attached to the active pet slot not the actual pet, as when you change pets, the button becomes useless, also the detachment and position needs to persist